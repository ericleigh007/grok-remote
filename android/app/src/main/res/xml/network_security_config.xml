<?xml version="1.0" encoding="utf-8"?>
<!-- Allow cleartext to Tailscale IPs / LAN while pairing; prefer HTTPS ts.net in production use. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
